---
galaxy_info:
  role_name: simple_dnsmasq
  author: bytesturm
  description: Installs and configures Dnsmasq to serve as local DNS cache and to resolve local domains for given hosts. The DHCP and TFTP functions are not activated.

  license: MIT License

  min_ansible_version: 2.9

  #
  # Provide a list of supported platforms, and for each platform a list of versions.
  # If you don't wish to enumerate all versions for a particular platform, use 'all'.
  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  #
  platforms:
    - name: Debian
      versions:
        - 10

  galaxy_tags:
    - dnsmasq
    - network
    - service
    - dns
    - simple

dependencies: []
